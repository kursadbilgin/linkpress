services:
  postgres:
    image: postgres:16
    container_name: linkpress-db
    environment:
      POSTGRES_DB: url_shortener
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - linkpress-data:/var/lib/postgresql/data

volumes:
  linkpress-data:
